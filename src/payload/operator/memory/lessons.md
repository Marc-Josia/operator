# Lessons

<!-- One-line lessons learned the hard way. Cap: 150 lines (`.operator/config.json`).

     Format — one entry per lesson, immutable ID:

     L-001: When <trigger>, do <action>, because <reason>. (source: <work-item id>)

     Example:
     L-001: When editing the billing worker, run the integration suite locally first,
     because its CI job is stubbed and misses queue regressions. (source: 007-billing-retry)

     Write triggers: op-fix records a lesson for every non-obvious root cause; op-ship's
     harvest promotes at most three durable items per work item. A lesson observed three
     times becomes a convention (C-NNN) and the lessons move to archive/. -->

_No lessons recorded yet._
