# Conventions

<!-- Numbered, durable rules this project follows. Cap: 200 lines (`.operator/config.json`).
     Load discipline: before touching code, read every rule whose `paths:` scope matches the
     files you will change; rules without `paths:` always apply.

     Format — one entry per rule, immutable ID, reason required:

     C-001 (paths: src/api/**): Validate all request bodies with the shared schema helpers,
     because hand-rolled validation caused the injection bug fixed in 004-input-validation.

     Write triggers: promoted from lessons seen 3×, harvested at ship, or recorded
     immediately when the operator states a rule. Every entry cites its source work item.
     When the file exceeds its cap, consolidate duplicates and move stale entries to
     `.operator/memory/archive/`. -->

_No conventions recorded yet._
